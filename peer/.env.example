# PocketDoc Laptop AI Provider Configuration

# Optional: Set a deterministic provider seed (64-character hex string)
# Leave unset to generate a new random public key on each startup.
# PROVIDER_SEED=
