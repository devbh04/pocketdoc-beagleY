# PocketDoc Board Server Configuration

# The port where the board HTTP + WebSocket server will listen
PORT=3000

# URL of the laptop AI provider (peer/provider.js)
# Replace <laptop-ip> with your laptop's actual LAN IP address
LAPTOP_AI_URL=http://<laptop-ip>:4000
